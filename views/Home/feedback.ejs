
<div class="container mt-5">
  <a href="/" class="h6">Home</a>
  <h2 class="mb-4 text-center text-info h1 bg-dark py-2 rounded">User Feedback</h2>

  <% if (feedbacks.length === 0 || feedbacks===undefined) { %>
    <p class="text-center text-muted">No feedback submitted yet.</p>
  <% } else { %>
    <% feedbacks.forEach(function(feedback, index) { %>
      <div class="card mb-3 shadow-sm">
        <div class="card-header bg-primary text-white">
          Feedback #<%= index + 1 %> - <%= feedback.Name %>
        </div>
        <div class="card-body">
          <p><strong>Email:</strong> <%= feedback.Email %></p>
          <p><strong>Message:</strong> <%= feedback.Feedback %></p>
          <p class="text-muted"><small>Submitted on <%= feedback.Feedback_DT %></small></p>
        </div>
      </div>
    <% }); %>
  <% } %>
</div>
